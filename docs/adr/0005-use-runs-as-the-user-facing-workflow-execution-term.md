# Use Runs as the user-facing workflow execution term

Studio uses Run as the user-facing term for dispatched workflow execution while preserving Workflow Instance for the durable execution record. This lets navigation and runtime UX speak in the user's language, such as Runs, Test Run, Published Run, and Run Kind, while existing route and API contracts can remain instance-oriented for compatibility.
